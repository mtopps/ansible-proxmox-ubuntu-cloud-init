# This file is an example of the secrets file.
# It is not used in the playbook.
# It is only used to show the structure of the secrets file.
# The actual secrets file is not checked into the repository.
# It is passed to the playbook via the --ask-vault-pass flag.
# To create the secrets file, run the following command:
# ansible-vault create vars/secrets.yaml

sshkey: "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5DDDSEFDthQd2cPM6/EnGwyTBI2eiErzU4MDK974oV9MhHs5S user@example.com"
cipassword: "a_really_complex_password"
